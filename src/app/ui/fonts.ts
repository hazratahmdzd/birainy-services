import { Space_Grotesk, Inter, Orbitron } from "next/font/google";

export const spaceGrotesk = Space_Grotesk({
  variable: "--font-space-grotesk",
  subsets: ["latin"],
});

export const rwDewiAlt = Inter({
  variable: "--font-rwdewi",
  subsets: ["latin"],
});
